<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Editar Contacto</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-800 p-8">
  <div class="max-w-md mx-auto bg-white p-6 shadow rounded">
    <h2 class="text-2xl font-bold mb-4">Editar Contacto</h2>
    <form action="/update" method="POST" class="space-y-4">
      <input type="hidden" name="originalPhone" value="<%= contact.phone %>"/>
      <div>
        <label class="block font-medium">Nombre</label>
        <input type="text" name="name" value="<%= contact.name %>" required class="w-full border px-3 py-2 rounded" />
      </div>
      <div>
        <label class="block font-medium">Email</label>
        <input type="email" name="email" value="<%= contact.email %>" required class="w-full border px-3 py-2 rounded" />
      </div>
      <div>
        <label class="block font-medium">Teléfono</label>
        <input type="text" name="phone" value="<%= contact.phone %>" required class="w-full border px-3 py-2 rounded" />
      </div>
      <div class="flex justify-between">
        <a href="/" class="text-blue-500 hover:underline">Cancelar</a>
        <button type="submit" class="bg-black text-white px-4 py-2 rounded">Guardar Cambios</button>
      </div>
    </form>
  </div>
</body>
</html>
